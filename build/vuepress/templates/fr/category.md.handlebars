---
metaTitle: {{{ name }}} | macOS defaults
---
# {{ name }}

{{# image }}
<img src="./{{ filename }}" alt="Une capture de {{ ../name }}" width="{{ width }}" height="{{ height }}"
  style="height: auto" />

{{/ image }}
> {{{ description }}}

{{# keys }}
- [{{ title }}](./{{ key }}.html)
{{/ keys }}

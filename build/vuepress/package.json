{
  "name": "macos-defaults-list-builder",
  "version": "0.0.1",
  "main": "build.js",
  "scripts": {
    "clean": "rimraf docs* dist*",
    "build": "yarn clean && node build.js",
    "build:prod": "yarn build && vuepress build docs --dest dist",
    "start": "yarn build && vuepress dev docs",
    "test": "jest"
  },
  "license": "MIT",
  "dependencies": {
    "handlebars": "^4.7.3",
    "vuepress": "^1.2.0",
    "yaml": "^1.7.2"
  },
  "devDependencies": {
    "jest": "25.1.0",
    "prettier": "1.19.1",
    "rimraf": "3.0.2"
  }
}
